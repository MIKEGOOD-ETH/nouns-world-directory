# nouns-world-directory

Filterable directory pulling live data from a published Google Sheet.

## v12
- Full-width **black** header with **white** text; added `CONFIG.site.stickyHeader` (default false).
- Category chip border reduced to **2px** (Tailwind `border-2`).
- Removed gray background behind header logo container (transparent now).
- Keeps static side art, mobile filters, tags under description, and black logo fallback.

## Deploy
- Build: `npm run build`
- Output: `dist`
